/**
 * Scan limits for bounded self-browsing.
 */

export const DEFAULT_SCAN_LIMITS = {
    max_scan_time_seconds: 180,
    max_scrolls: 25,
    max_jobs: 100,
    max_posts: 100,
    scroll_step_fraction: 0.9,
    post_scroll_wait_ms: 1200
};
